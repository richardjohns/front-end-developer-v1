import React from 'react'

import { CountryCodeTable } from '../../containers'

const style = {
  margin: 0,
  padding: '1rem'
}

const App = () => (
  <div style={style}>
    <CountryCodeTable />
  </div>
)

export default App
